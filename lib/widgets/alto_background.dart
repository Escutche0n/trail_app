import 'package:flutter/material.dart';

/// 纯黑背景
class AltoBackground extends StatelessWidget {
  const AltoBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.black);
  }
}
